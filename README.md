# HMC-Computer-Vision-Project

It seems we need python 3.13 of some sort, as there as not stuff compatible with 3.14, but oh well.

Probably should create a venv. Then you can install the necessary packages using 
```pip install -r requirements.txt```
Pretty simple!

More to add later on...